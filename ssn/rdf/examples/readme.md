# Extended SOSA examples

This folder contains long running examples previously containted within the RDF files in this directory correspond to the numbered examples in the [Semantic Sensor Network Ontology](https://www.w3.org/TR/vocab-ssn/) recommendation. 


##Tracking Beer Temperature with IBS-TH2

This example is documented in detail in ACME-Beer-Example.md 
An RDF file containing a (graph)[Beer-Full-IBS-TH2.ttl] corresponding tothis example is available

## Tree height measurement

An RDF file containing a (example)[tree-height.ttl]graphcorresponding to this example is available

##Number of sunspots

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/sunspots.ttl]graphcorresponding tothis example is available

##Seismograph measuring ground displacement speed

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/seismograph.ttl]graphcorresponding to this example is available

##iPhone Barometer

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/iphone_barometer-sosa.ttl]graphcorresponding to this example is available

##Temperature and Humidity at Coal Oil Point Reserve

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/IDEAS.ttl]graphcorresponding to this example is available

##Electricity consumption of apartment #134

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/apartment-134.ttl]graphcorresponding to this example is available

##Wind sensor spinning cups

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/spinning-cups.ttl]graphcorresponding to this example is available

##Complex sensor capabilities &mdash; DHT22

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/dht22.ttl]graph    corresponding to this example is available

##DHT22 Deployment

This example shows how the conditions (temperature and humidity) in a room can be measured using one or more sensors.
Each sensor observes the conditions in its immediate vicinity, and the values are then used to characterize the room.

In Room 145, one of the walls is external in the building, so there is expected to be a temperature gradient across the room, and there are two sensors on different walls. In Room 245, there is one sensor on the south wall. Each of these locations corresponds to a `sosa:Sample` of the entire room. The wall also serves as a `sosa:Platform` on which the sensors are mounted. </p>

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/dht22-deployment.ttl]graph corresponding to this example is available

##IP68 Smart Sensor

This example describes the IP68 Smart Sensor that and some of its capabilities and operating ranges. A specific IP68 Smart Sensor observes the air temperature, and its own battery state.</p>

An RDF file containing a ()[https://github.com/w3c/sdw/blob/gh-pages/sdw-sosa-ssn/ssn/rdf/examples/ip68.ttl]graph corresponding to this example is available.


